Run a code review on $ARGUMENTS using the bridge pipeline (Gemini → Codex), regardless of file size.

Use the Bash tool to run:
```
aitools review $ARGUMENTS
```

Then present the full output to the user.
